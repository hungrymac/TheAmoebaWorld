{
  "name": "amoeba-saas-platform",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "db:generate": "pnpm --filter @amoeba/db exec drizzle-kit generate",
    "db:studio": "pnpm --filter @amoeba/db exec drizzle-kit studio"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@10.33.3",
  "engines": {
    "node": ">=22"
  }
}
